# Secret Shield — Gitleaks (extends defaults + project allowlist)
title = "Haravan Claw Master (openclaw-haravan)"

[extend]
useDefault = true

[allowlist]
paths = [
  '''\.gitleaks\.toml$''',
  '''scripts/security-scan\.mjs$''',
]
