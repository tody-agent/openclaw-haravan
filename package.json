{
  "name": "openclaw-haravan",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw x Haravan — Trợ lý vận hành cửa hàng Haravan cho nhóm non-tech qua AI Agent.",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^4.1.2",
    "vitepress": "^1.6.4"
  }
}
