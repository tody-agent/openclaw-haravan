{
  "name": "openclaw-haravan",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw x Haravan — Trợ lý vận hành cửa hàng Haravan cho nhóm non-tech qua AI Agent.",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "test:gate": "npm run build --workspaces --if-present && npm run docs:build && vitest run --reporter=verbose",
    "security:scan": "node scripts/security-scan.mjs",
    "verify": "npm run security:scan && npm run test:gate",
    "hooks:install": "git config core.hooksPath .githooks",
    "docs:dev": "npx vitepress dev docs",
    "docs:build": "npx vitepress build docs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.7",
    "typescript": "^5.3.3",
    "vitest": "^4.1.2",
    "vitepress": "^1.6.4"
  }
}
