## Summary

<!-- What does this PR change and why? -->

## Checklist

- [ ] `npm run verify` passes locally (or equivalent: `security:scan` + `test:gate`)
- [ ] No secrets committed (`.env`, `.dev.vars`, tokens in source)
- [ ] Docs updated if user-facing behavior or install steps changed
